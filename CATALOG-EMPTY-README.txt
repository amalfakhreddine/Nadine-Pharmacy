NADINE PARAPHARMACY — EMPTY CATALOG BUILD

This build contains zero bundled products.
After deployment, the customer storefront is empty immediately.
On the first authorized admin login, the old products collection is permanently cleared once, including products previously in Trash, and category lists are reset.
The reset is versioned so products imported later are NOT deleted on future logins.

Deploy the entire ZIP. Then sign in to Admin once and wait for the Products page to load.
