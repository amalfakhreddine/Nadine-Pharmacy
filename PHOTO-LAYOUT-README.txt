PRODUCT PHOTO LAYOUT

Upload this folder's contents to your existing repository, including
product-photos.css. This version includes the previous price fix.

Storefront cards, related products and the product detail gallery use a square
white frame. The whole source image fits inside the frame with consistent
padding. Mobile cards no longer rely on a fixed-height image area. Automatic
search thumbnail URLs request uncropped images at up to 1000 x 1000 pixels.
Larger requested dimensions do not create detail absent from the source.

PHOTO SELECTION STILL REQUIRED
The supplied spreadsheet has no approved Image URL values. The existing catalog
uses automatic search thumbnails, which can include people, hands, incorrect
products and low-quality photos. This update does not claim those 2,236 images
have been individually reviewed or replaced. Existing photos are retained.

For the requested finished catalog, replace search thumbnails with verified
product-only photographs: white background, no hands or people, full packaging,
ideally 1000 x 1000 pixels with the product occupying roughly 76% of the frame.
Source whitespace affects apparent product size even with matching frames.
Use the existing admin image upload to assign approved photos to products.
